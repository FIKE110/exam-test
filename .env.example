# IMPORTANT: Run db/schema.sql before starting the application!
# psql -U postgres -d exam_preparation -f db/schema.sql
#
# Application
NODE_ENV=development
PORT=3001
FRONTEND_URL=http://localhost:3000

# Database
# For local PostgreSQL, set these values:
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=exam_preparation

# Or use a full connection string (overrides individual settings):
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/exam_preparation

# JWT Configuration
JWT_SECRET=your-jwt-secret-key-change-this-in-production
JWT_REFRESH_SECRET=your-refresh-secret-key-change-this-in-production

# AI Configuration (OpenAI)
OPENAI_API_KEY=your-openai-api-key

# File Storage (AWS S3)
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_S3_BUCKET=your-bucket-name
AWS_REGION=us-east-1

# Email (for notifications)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASSWORD=your-email-password

# Redis (for caching)
REDIS_URL=redis://localhost:6379
